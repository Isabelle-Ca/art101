// index.js - Lab 4 - Pseudocoding and Problem-solving
// Author: Isabelle Carrou
// Hangman
// Guessing Game
// Isabelle Carrou <icarrou@ucsc.edu>
// October 2024

// Constants

// Functions

// this is an example function and this comment tells what it doees and what parameters are passed to it.
function myFunction(param1, param2) {
  // some code here
  // return results;
}

function main() {
  console.log("Main function started.");
  // the code that makes everything happen
}

// let's get this party started
main();

//Take input_string from user1 
    //Loop through input_string, increase count by 1 for every letter string contains (input_string = cat, count = 3)
    //Print new string “_” for number in count (count = 3, new_string = “_ _ _”)
//Loop until input_string = new_string
    //Take input_char from user2
        //Iterate through input_string
            //If input_char is in input_string
              //Update input_char at index in new_string
              //Print new string (char = c, new_string = “c _ _”)
        //If input_charis not input_string
              //Increase strike_count by 1
              //Print updated hangman
              //If strike_ count = 6
                //Print player lost message
                //Close program
//Print player won message
//Close program